copy "trainingdata.mat" to the current folder.

Run optimize_ss.m to generate "workspace_gatecombinations_rpm_networA.mat" which contains all the RPM models that closely reproduced the training dataset.