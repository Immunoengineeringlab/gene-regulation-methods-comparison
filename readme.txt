data needed to plot each figure is generated by running the scripts in specified folder. Each folders and subfolders contain separate readme files explaining the order of execution of matlab script files to generate data and to plot.